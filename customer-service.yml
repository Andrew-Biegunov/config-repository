feign:
  circuitbreaker:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: 'health,info,refresh'
spring:
  cloud:
    loadbalancer:
      ribbon:
        enable: false
  data:
    mongodb:
      database: customer-db
      host: localhost
      port: 27017
